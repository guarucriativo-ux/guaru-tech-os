# Modelo de variáveis de ambiente do Guaru Tech OS.
# Copie este arquivo para .env e preencha com os valores reais.
# O .env NUNCA deve ser versionado (está ignorado no .gitignore).

ANTHROPIC_API_KEY=sua_chave_aqui
