*.mp4
*.MOV
*.mov
*.rar
*.psd
*.zip
node_modules/
.DS_Store

# Segredos / credenciais — nunca versionar
api_key/
*.key
key.txt
.env
.env.*
!.env.example

# Arquivos > 100MB que o GitHub rejeita (limite rigido)
Projetos/Alkimia/Ref. Catalogo/CATALOGO_FINAL (1).pdf

# Snapshot do repo (gerado por tools/gerar-snapshot.mjs sob demanda p/ anexar a IA externa).
# Regenerável e pesado — nao versionar; rodar o script quando precisar reanexar.
SNAPSHOT-REPO.md
